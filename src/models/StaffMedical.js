export class StaffMedical {
    constructor(matricule, nom, adresse, tel,password) {
      this.matricule = matricule;
      this.nom = nom;
      this.adresse = adresse;
      this.tel = tel;
      this.password = password; 
    }
  }
  